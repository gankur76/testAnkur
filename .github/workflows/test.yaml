name: Test Envirinment
on : [push]

jobs:
  test-environments:
    runs-on: ubuntu-latest
    steps:
    - name: check environment variable
      env:
        MY_SEC: '${{ secrets.MY_SEC1 }}'
      run: |
        echo $MY_SEC >> secrets.txt
        cat secrets.txt